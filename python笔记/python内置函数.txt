真--True

假--False

取得数据变量类型--type()

断言--assert

模块内声明全局变量--global

避免错误运行--try/except

避免错误运行子句--else:   ,   finally:

返回错误值--raise

计算传入的参数的值--eval('需要计算的参数')

查看字符串的长度--len()

查找字符串中的子串返回最左端索引--find()

把序列转换为字符串--join()

替换字符串中的匹配值--replace()

将字符串分割为列表--split()

替换字符串中的单个字符--translate()

比较2个对象,x<y返回-1,x==y返回0,x>y返回1--cmp(x,y)

返回四舍五入小数,第二位参数设定显示几位数,负数返回10的倍数--round(小数,-1）

返回给定参数中最大元素--max()

返回给定参数中最小元素--min()

将字符串转换为列表--list()

把所有字母转换为大写--.upper()

把所有字母转换为小写--.lower()

把值转换为合理的python表达式--repr()

把值转换为用户容易理解的字符串--str()

把首字母及空格或标点符号后的首字母转换为大写--.title()

把字符串中首字母大写，其他都转换为小写--.capitalize()

检查字符串中的内容是否全部由字母构成--.isalpha()

检查字符串中的内容是否全部由数字构成--.isdigit()

更好的打印格式--import pprint  pprint.pprint(需要打印的字符）

打印居中--.center(字符总长度)

打印左对齐--.rjust(字符长度)

打印右对齐--.ljust(字符长度)

换行打印--\n

制表符--\t

删除字符串两侧字符串空格或其他字符--.strip()

删除字符串末尾空格或其他字符--.rstrip()

删除字符串开头空格或其他字符--.lstrip()

查找一个字符(字符串)在另外一个字符串中出现的次数--.count()

查找一个字符串在另外一个字符串中第一次出现的位置--.find()

替换字符串,u替换m，.replace('u','m')--.replace()

得到原始输入--input()

所有输入为字符串--raw_input()

把字符串转换成浮点数--float()

把字符串转换成整数--int()

返回数字绝对值--abs()

或者不是另一个-- or not

插入格式函数配合花括号｛填充键值｝使用--format()

插入格式函数功能类似format()--repr()

把序列转换为元组--tuple()

获取每个元素的索引及其值--enumerate(列表)

找到列表中的最小值--min()

找到列表中的最大值--max()

得到列表中的数字之和--sum()

对参数序列中的元素进行积累      --reduce(lambda x,y:x+y,[1,2,3])

以参数中的每一个元素调用a函数，
返回每次a函数返回值的新列表     --map(a,[1,2,3])  map(lambda x,y:x+y,[1,2,3],[1,2,3])

统计列表中某个项的数目--count()

统计列表中某个项第一次出现的位置--index()

在列表末尾添加一个项--append()

把一个列表的项添加到另一个列表的结尾--extend()

移除列表中的一个值--pop()

删除列表的一个项或值--remove()

清空列表 --.clear()

在列表的指定位置添加项--insert(索引，'值')

反转列表中所有项的顺序--reverse()

序列a中的元素相加后加b，b默认0--sum((a,b))

把列表中的项按字母或数字的顺序排列--sort()

把列表中的项按字母或数字的顺序反向排列--sort(revers=True)

删除重复数据加排列数据--set()

两个合集a,b的并集--a.union(b)

返回列表--list()

把序列中的项排序返回列表副本--sorted()      sorted(revers=True)

把序列的项按i[0]排序--sorted(列表的列表,key=lambda i:i[0]) 

元组--tuple()

循环一定次数或列表中的每个项运行一次--for

接受一个数字返回一个有规律的数字列表--range()

压缩函数--zip()

类似于range,一次只创建一个数--xrange()

跳到下一个列表项继续循环--continue

退出循环--break

返回抛出s,i是大写--raise StopIteration

编码--encode('utf-8')

解码--decode('utf-8')













